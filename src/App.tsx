import MessageContainer from './components/message-container';
import MainRouter from './routes/main-router';

const App = () => {
  return (
    <MessageContainer>
      <MainRouter />
    </MessageContainer>
  );
};

export default App;
